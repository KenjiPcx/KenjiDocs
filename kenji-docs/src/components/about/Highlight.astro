---
const colors = [
  "text-green-600",
  "text-emerald-600",
  "text-teal-600",
  "text-sky-600",
  "text-blue-600",
  "text-indigo-600",
  "text-violet-600",
];

const color = colors[Math.floor(Math.random() * colors.length)];
---

<strong class={color}><slot /></strong>
